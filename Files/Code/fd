PRIVMSG ##UPM :declare -x resultb="r alphabets),   -e program        one line of program (several -e's allowed, omit programfile),   -E program        like -e, but enables all optional features,   -f                don't do \\\$sitelib/sitecustomize.pl at startup,   -F/pattern/       split() pattern for -a switch (//'s are optional),   -i[extension]     edit <> files in place (makes backup if extension supplied),   -Idirectory       specify @INC/
